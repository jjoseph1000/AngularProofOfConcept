export interface IDayCell {
    cellText: string
    cellSpan: number
    cellColor: string
    popupCaptionText: string    
}

export class DayCell implements IDayCell {
    cellText: string
    cellSpan: number
    cellColor: string
    popupCaptionText: string    
}